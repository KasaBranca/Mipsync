#pragma once

#include "CommandTypes.h"
#include <string>

namespace MipsyncEngine::Command {

std::string RenderHuman(const std::string& command, const CommandResult& result);

} // namespace MipsyncEngine::Command
