## Mipsync Engine 0.1.0

Windows x64 editor build.

### Install
1. Download `MipsyncEngine_Windows_x64.zip`
2. Extract anywhere
3. Launch `MipsyncEngine.exe` (or install via Mipsync Hub → Installs)

### Contents
- `MipsyncEngine.exe`
- MinGW runtime DLLs
- `resources/` (icons, fonts)
